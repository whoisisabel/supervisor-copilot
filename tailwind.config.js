/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        urbanist: ['var(--font-urbanist)'],
        worksans: ['var(--font-work-sans)'],
      },
    },
  },
};